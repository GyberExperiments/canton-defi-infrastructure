apiVersion: v1
kind: Service
metadata:
  name: multi-swarm-system-prod-service
  namespace: default
  labels:
    app: multi-swarm-system
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: multi-swarm-system
    environment: production
